# Dashboard build-time config. The admin secret is NOT baked in — it's entered
# at runtime and kept in the browser's localStorage.
VITE_FUNCTIONS_URL=https://fdrdecpzihlztncvglwx.supabase.co/functions/v1
